1 call to function:

function findIndex(arr, val){}

(variables we added)
return address
(val)
(arr)

^ all one stack


base case - case that terminates recursion

recursive case - can call itself many times

scope variables - 



Helper Method Recursion - novice
	do not have to return result up the stack
	allowed flexin num of args
	scope variabvles axx anwhere on call stack
	struct to solve most rec. probs


Recursion w/ side effects (intermediate) - any inputs you have are the same you will ever have



range between - 40% and 60% of leetcode